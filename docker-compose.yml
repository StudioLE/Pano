version: '3.3'

services:
  nginx:
    container_name: pano
    image: nginx:1.17.8-alpine
    volumes:
      - ./public:/usr/share/nginx/html
    labels:
      domain: pano.studiole.uk
    networks:
      - traefik
networks:
  traefik:
    external:
      name: traefik_web